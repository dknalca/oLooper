{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege baseline: core + native file dialogs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
