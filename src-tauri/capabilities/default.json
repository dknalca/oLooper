{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege baseline: core app only, no fs/dialog access yet.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
